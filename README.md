# mermaid-flutter

Flutter app for Mermaid Resort.

## Setup

1. Ensure Flutter SDK is installed.
2. From project root run:

```bash
flutter pub get
flutter run
```

## Assets
All images are in the `assets/` folder and declared in `pubspec.yaml`.
